1.将24、25行换成自己的
2.更改75行的ApplicationType即可实现你的第四个问题



以下是研究过程
1、首先调整find_elements直接获取一整页的元素，而后每页的十个依次点进去拿日期------不行
原因：chrome的问题，进入日期页面返回后重定向到第一页
解决办法：更换成edgedrive
2、使用edge继续获取数据，由于频繁的切换网页，导致代码质量不稳定，几乎很难完全跑完------不行
解决办法：更改爬取思路，观察到获取日期的页面可以由固定地址与Application_Number组合得到，故前三样与日期分别获取
3、为了不添麻烦更换为chromedrive采用方法2，由于VPN网络问题有小概率出现找不到元素，多试几次就行。


问题2的解决
控制循环次数，搜索出329个结果，每页10个共有33页，因此需要翻页32次，但别的搜索结果可能不是33页，因此尝试获取搜索结果数----失败
转而获取最下方next按钮左边的最大数字----成功，故可以精确控制循环次数，不会多拿或者少拿。

由于爬取时间过长，约360个网页访问，故没有录屏